# Pre-runner asset production tranche

**Date:** 2026-08-30  
**Base:** `develop` at `04a34e37f4f0c2f982cf28b942e11e120d103d29`  
**Branch:** `feature/pre-runner-asset-production` / PR #18  
**Stop boundary:** continue until the next required work would need unauthored main-story/Quest/Turn/Standing identities or a runner/editor fact that cannot be produced from repository authority.

## Operator direction

Continue on the Rockstar path. Do not optimise for a short route to an Alpha label. Use the already-adapted Claude Code Game Studios methodology and NVIDIA/Kimi only as bounded production assistants. Use Higgsfield as the upstream non-canon visual/motion asset and previsualisation lane for work the connected provider can actually perform. Unreal remains the later integration/runtime/packaging authority. Final voice remains a later, separately gated production stage.

The intended overall sequence is:

1. finish authored source/content/asset briefs and permitted external previs/candidates;
2. stop rather than invent unresolved main-story or mission identities;
3. later bring UE 5.5 Windows/Linux runner/editor capability online;
4. import/integrate/review the real game content;
5. build content-identical Windows + Linux Alpha packages from the same accepted commit;
6. perform complete private playthrough/exploration through the chosen Arcware-style streaming path;
7. record defects/weaknesses and run a dedicated improvement tranche.

The later streaming playthrough is a quality/acceptance loop and does not replace native Windows/Linux evidence.

## Current repository fact check

- `develop` baseline for this tranche is PR #17 merge `04a34e37f4f0c2f982cf28b942e11e120d103d29`.
- `CLAUDE.md` exists and configures the adapted `Donchitos/Claude-Code-Game-Studios` workflow.
- `.claude/agents/*`, `.claude/skills/team-level/SKILL.md` and `.github/workflows/game-studios-kimi.yml` already exist; do not duplicate them.
- Higgsfield workspace is on the Free plan with 10 credits and Unlimited false.
- Useful image/video model families are discoverable through the connected provider.
- Two Seedance 2.0 Mini 4 s / 480p submission attempts returned `Requires basic plan or higher` before any job was created; zero credits were consumed.
- Connected 3D-model discovery did not return a usable 3D generation model/action. Treat 3D mesh/rig generation as unavailable through the current connection until a concrete supported provider action exists; do not pretend a GLB was created.
- No provider upgrade, credit purchase or paid generation may be started automatically.

## Work order

1. **Governance reconciliation**
   - keep `HANDOVER.md`, `Docs/ALPHA_DELIVERY_CHECKLIST.md` and agent rules aligned with the PR #17 baseline and PR #18 work;
   - preserve source/runtime/asset evidence separation;
   - record that `CLAUDE.md` and Game Studios are present.

2. **External asset production authority**
   - maintain the native C++ production catalog mapping canonical requirements to output/evidence classes;
   - keep concept/reference image, motion-previs video, cinematic previs, facial/performance reference and candidate mesh/rig/motion classes distinct;
   - candidate 3D classes may exist as evidence types even when no connected 3D provider model is currently available;
   - never mark an external generation as a UE production asset until actual import/review/runtime evidence exists.

3. **Finite Higgsfield brief coverage**
   - 23 animation requirements;
   - 14 resolved cutscene identities, excluding 5 unresolved final-act slots;
   - 40 grounded named dungeon production profiles, excluding 20 unauthored minors and the unresolved named Region 06 slot;
   - 8 world-region identities;
   - 12 Highmoore named anchors;
   - 9 State Treasures + the currently grounded unique reward;
   - then source-reconnaissance batches for characters, creatures/bosses and La Liberación.

4. **Animation/presentation production briefs**
   - derive provider-safe briefs from existing canonical animation/presentation catalogs;
   - do not invent Jake/crew visual identity where the repository has no approved source-backed visual fact;
   - for combat, provider video is motion reference only unless a real candidate motion artifact later exists and is imported/reviewed;
   - preserve 8-frame attack tell, 6-frame deflection, committed recovery, wound layers, horse gait ownership and Katana exception.

5. **World/dungeon/prop production readiness**
   - use actual dungeon production profiles rather than generic fantasy prompts;
   - preserve Access/Discovery, Puzzle, Hazard, Unforgettable Image, Boss/Bottom and Reward fields; withheld remains withheld;
   - never invent layout coordinates, extra chambers, bosses, settlements, landmarks, treasure identities, child remains, ambient dungeon music or map-marker language;
   - keep all `.umap`/`.uasset` claims false until real UE evidence exists.

6. **Character / boss / creature / ship visual reconnaissance**
   - read each governing design source before authoring a provider brief;
   - include only explicitly sourced visual/body/costume/equipment/performance facts;
   - if a provider would need an unspecified fact, omit it or record `DESIGN-GAP` instead of completing the prompt with generic AI assumptions.

7. **Quest/main-story boundary reconnaissance**
   - re-scan authored quest sources only after the pre-quest asset/content families above are exhausted;
   - do not create any of the 129 missing Turn identities or 146 missing Standing identities;
   - do not invent missing main-story triggers, actors, outcomes or sequence identities;
   - stop this tranche when further progression requires those unauthored identities and report the exact gap/source.

## Evidence classes

Every external-media record must use one of these states:

- `RequirementOnly` — canonical requirement exists, no external generation.
- `PrevisGenerated` — external image/video/reference exists, not a game asset.
- `CandidateAssetGenerated` — mesh/audio/etc. candidate exists, still unimported/unreviewed.
- `ImportedForReview` — real Unreal import exists, but no runtime acceptance yet.
- `RuntimeAccepted` — runner/editor/runtime evidence exists on the exact commit.

Only the last two states imply actual Unreal content exists, and `RuntimeAccepted` requires evidence.

## Cost and rights law

- No provider upgrade, credit purchase, paid generation, licensing purchase or billable resource without explicit operator approval.
- Existing free credits do not override a provider-plan block.
- External output may be used as non-shipping previs before shipping rights are cleared, but cannot be called shipping/commercially cleared until provider terms and project use are explicitly accepted.
- Voice remains blocked for shipping until dialogue lock, casting role, pronunciation record, subtitle text and rights/cost approval exist.

## Merge boundary

This tranche may merge source/docs/tests that improve production authority and provenance only when the operator explicitly authorizes the merge for this action. It may not claim any generated media, UE compile, automation, final assets, 60 fps, package, playthrough or Alpha acceptance that was not actually produced and evidenced.
